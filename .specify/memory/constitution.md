<!--
Sync Impact Report — Constitution v4.0.0
============================================
Version change: 3.0.0 → 4.0.0 (MAJOR)
Modified principles:
  - §一 技术栈不变量：简化为只锁大层（React+Tailwind+TS / Node.js+TS+PostgreSQL / pnpm）
  - 删除具体库锁定（Hono、Drizzle、Zustand、React Router 等降为实现细节）
  - 删除 Golang 迁移计划（全栈统一 TypeScript）
  - 技术约束表同步简化
Removed sections: 无（仅简化和删除具体条款）
Templates requiring updates:
  - .specify/templates/plan-template.md — ✅ 无需变更
  - .specify/templates/spec-template.md — ✅ 无需变更
  - .specify/templates/tasks-template.md — ✅ 无需变更
  - .specify/templates/checklist-template.md — ✅ 无需变更
  - .specify/templates/agent-file-template.md — ✅ 无需变更
Follow-up TODOs:
  - CLAUDE.md 需同步更新
  - README.md 技术栈表需同步更新
-->

# 盘根 · AI 指南针（PanGen AI Compass）项目宪法

## 当前目标架构一致性裁定（2026-04-25）

本节用于消除旧双站口径与当前目标架构的冲突。若本文后续旧段落仍出现“手机号 + 验证码主注册”“两站账号完全独立”“校园 PRD v8”等历史说法，以本节和 `specs/架构设计.md` 为准：

- 账号身份统一；账号级 level / LV 共享。
- 校园 profile、全球 profile、知识库、方案、收藏和业务行为数据分离。
- 早期注册只要求用户名 + 密码；邮箱、手机号、GitHub OAuth 是后置绑定或准入补充。
- 不实现手机号/SMS 主注册分支。
- 跨域 Cookie/token 不直接共享；站间跳转不携带登录态。
- cn 原始个人数据不流向海外；com 站不收集中国敏感个人信息。
- 校园站当前 PRD 为 `specs/PRD-盘根校园-v9.md`。

## 公理层（Axioms）

公理是项目最高约束，不可被下游任何原则、规格或决策覆盖。
仅能通过宪法修订流程（MAJOR 版本）变更。

### 〇、五条公理

| 公理 | 内容 | 说明 |
|------|------|------|
| **人类主权** | AI 处理逻辑、组织、格式化；人类掌握价值判断、审美判断、最终决策 | AI 不自动发布、不自动精选、不替代人的判断。AI 的工作是提问和整理，人的工作是思考和决定。所有 AI 输出必须经人工确认才生效。 |
| **熵减命令** | 系统唯一目的：将混乱/未知转化为秩序/已知 | 每个功能必须回答"它减少了什么混乱？"。无法回答的功能不做。信息从无序到有序是系统的唯一价值方向。 |
| **多元世界观** | 蓝图（应然）与现实（实然）并存，偏差驱动行动 | 计划是蓝图，执行是现实。系统同时呈现两者，偏差是改进的信号而非错误。不追求蓝图完美，追求偏差可见、行动可追踪。 |
| **量变质变** | 数据积累 → 分析 → 改进，量变引发质变 | 没有数据支撑的决策是猜测。系统必须先采集、再分析、最后改进。不跳过积累阶段，不用直觉替代数据。 |
| **做减法** | 最短路径即最优路径 | 能 1 步完成的不用 2 步。能 1 个按钮解决的不放 2 个。每新增一个功能，必须删除一个旧功能或简化一个流程。复杂是能力的敌人。 |

**适用范围**：所有产品设计、技术决策、功能优先级判断必须先通过公理检验。
当核心原则与公理冲突时，以公理为准。

## 核心原则

### 一、技术栈不变量

全栈统一 TypeScript，不引入其他语言。两站共享同一技术栈。

| 层 | 锁定技术 |
|---|---|
| 前端 | React + Tailwind CSS + TypeScript |
| 后端 | Node.js + TypeScript + PostgreSQL |
| 包管理 | pnpm |

具体库选型（路由、ORM、状态管理等）为实现细节，不写入宪法，
由实现阶段按需决定，偏离本表才须经修订流程审批。

**不写兼容代码**：如无显性兼容性要求，禁止编写额外兼容代码（如旧浏览器
polyfill、降级分支）。优先使用现代浏览器能力，以桌面 Chrome、移动端
Safari / Chrome 为主。

**共享包约束**：`@ns/shared` 必须保持纯 TypeScript，不得引入 React
或任何 UI 依赖。

**原型阶段**：前端允许使用 localStorage + Mock 数据闭环体验。
待需要多人协作或成本治理时再引入后端服务与数据库——原型阶段不以后端为阻塞。

### 二、安全与密钥不变量

- 前端构建产物不得包含任何第三方模型厂商的真实 API Key（明文或混淆均禁止）。
- 开发环境：API 请求仅通过 Vite dev proxy 转发。
- 生产环境：所有 AI 调用必须经后端 AI 网关，由网关持有 Key。
- 含密钥的本地配置文件（`.env.*.local`、`.zhipu.local.json` 等）必须在
  `.gitignore` 中声明，绝不提交到版本控制。

**原因**：API Key 泄露直接造成经济损失，威胁项目运营安全。

**AI 资源治理**：AI 搜索 MUST 从第一天起实施限流，先严后松。
- 未登录用户：每日搜索上限 MUST 低于登录用户。
- 登录用户：有明确额度，额度耗尽给出提示而非静默失败。
- 异常检测：同一 IP 短时间大量请求 MUST 自动拦截。
- 额度标准上线后可调整，但不允许存在"不限量"状态。

**原因**：AI 调用按次计费，无防护直接暴露于公网等于财务风险。

### 三、文档与语言不变量

- 文档（规格、计划、变更记录）尽量使用中文编写。
- 代码注释尽量使用中文编写。
- 变量 / 函数 / 类型命名使用英文驼峰（camelCase / PascalCase）。
- 本宪法以中文为权威文本。

**原因**：团队沟通以中文为主；代码标识符保持英文以兼容生态与工具链。

### 四、产品体验不变量（Apple 式克制）

- **最短主路径**：任何页面最多 1 个主按钮；主按钮表达"下一步行动"。
- **渐进披露**：默认只展示决策所需信息，细节通过抽屉 / 展开呈现。
- **一致性优先**：同类组件在不同页面保持一致（错误态、空态、确认弹窗）。
- **反馈及时**：关键操作必须给出 loading / 成功 / 失败 / 不可撤销提示。
- **禁止**：通过"仪式感文案 / 装饰性动效"制造价值感。
- **冷启动诚实策略**：内容不足时，AI 搜索无结果 MUST NOT 编造内容。
  无结果时必须展示相关已有内容 + 收集用户需求入口。每个领域至少
  有 10 篇内容后才正式开放（开放前使用 Locked 态）。
- **UI 图标规范**：所有 UI 图标统一使用 Lucide icon 库（SVG），禁止使用
  emoji 作为 UI 元素。emoji 仅允许出现在用户输入的文本内容中（帖子正文、
  评论）。知识库图标、Feed 类型标识、按钮、导航、状态等必须使用 Lucide。

**原因**：用户群包含非技术用户；清晰可预期减少支持负担、建立信任。emoji 跨平台渲染不一致，SVG 图标专业、可控、一致。

### 五、多端交付不变量

- 桌面端：Web（浏览器）为主形态。
- 移动端 App：独立设计、独立开发，与 Web 完全不同，仅通过 API 共享数据。
- App 技术选型不与 Web 绑定，待 Web 站稳定后单独评估。
- Web 端移动适配底线：触控目标 ≥ 44×44 px；Safe Area 不遮挡；
  键盘弹出不挡主操作。

**原因**：Web 是核心产品（信息网站），App 是配套产品。两者 UI/交互
天然不同，强求统一会两头不讨好。共享 API 数据层即可。

### 六、功能阶段化上线不变量（Feature Flags）

所有非核心能力必须可阶段化点亮，统一三态策略：

1. **Hidden（隐藏）**：入口不可见。
2. **Locked（锁定态）**：入口可见但不可用，点击给出解释与预期上线时间。
3. **Degraded（降级可用）**：功能可用但质量 / 额度 / 范围降低，必须有明确提示。

核心能力（阅读内容、导航、基础搜索）免于 Feature Flag 门控。

**原因**：分阶段上线防止未完成功能污染用户体验，同时提供安全的回滚手段。

### 七、内容与文档标准不变量

- 系统内部唯一标准内容格式：Markdown。
- 允许受控扩展块（白名单）：例如 `:::video{...}:::`、`:::callout{...}:::`。
- 禁止任意 HTML 直出（安全与样式失控）。

**原因**：Markdown 保持内容管道确定性，避免未过滤 HTML 带来的 XSS 风险。

### 八、后台与协作不变量

- 系统后台（`/admin`）是高权限区：仅 reviewer / editor / admin 可访问；
  普通作者不可见。
- 内容发布必须审核通过（可由任意授权审核员完成，不强制本人）。
- 发布后允许修改，但必须走二次审核；线上旧版本保持可见，新版本审核通过后替换。
- 后台 AI 只做"建议 / 草稿 / 辅助分析"，禁止自动发布、自动精选、自动下架。
- 指派 / 改派 / AI 操作 / 版本回滚必须写审计日志。

**原因**：内容完整性与问责机制对面向用户群体的平台不可妥协。

### 九、本地存储不变量

- 读写必须有校验（guard），数据损坏自动重置。
- 重置必须提示一次且文案固定："本地数据异常，系统已重置到默认状态。"
- 写入必须满足原子性：一次 `setItem` 写入完整 JSON，禁止分多次局部更新。

**原因**：原型阶段 localStorage 是当前持久层，保证数据完整性可防止脏数据引发级联 UI 错误。

### 十、参考规格与单一事实来源

- 使命文档：`specs/MISSION.md`（定义"要做什么"，位于宪法与 PRD 之间）
- 校园站 PRD：`specs/PRD-盘根校园-v9.md`
- 全球站 PRD：`specs/PRD-盘根AI指南针-标准版.md`
- 变更规格：`specs/` 下各功能目录。
- 本宪法是所有开发决策的最高约束文件。规格文档与宪法冲突时，以宪法为准。

### 十一、双站架构：成为自己

盘根是一个人成为自己的路。校园站帮你站稳脚下，全球站帮你看见远方。
两个站点不是对立面，而是同一个人不同阶段、不同需求的两种支撑。

项目采用双前端架构，两个站点共享产品能力，仅内容领域和数据合规不同。

#### 核心理念：成为自己

- **校园站（xyzidea.cn）**：以校园为锚点的区域知识平台——日常起居 /
  成长提升 / 精明消费。帮你站稳脚下，过好当下。
- **全球站（xyzidea.com）**：AI 前沿知识、工具方法、创新实践。
  帮你看见远方，把 idea 变成现实。

两站共享产品能力与交互体验，面向不同场景和用户群体。底层由 Ferry
框架（熵减思维：规划→执行→复盘）指导两站的内容方向与质量保证。

#### 站点定义

| 属性 | xyzidea.cn（校园站） | xyzidea.com（全球站） |
| --- | --- | --- |
| 定位 | 区域知识平台（以校园为锚点，服务所在区域所有人） | 全球 AI 知识平台（前沿资讯 + 工具方法 + 创新实践） |
| 域名 | `xyzidea.cn`（国内服务器，ICP 备案） | `xyzidea.com`（海外服务器） |
| 入口 | `frontlife-web` 独立包（可运行，mock 数据） | `frontai-web` 独立包（可运行） |
| 目标用户 | 校园所在区域的全体居民（学生、教职工、当地居民） | 全球想用 AI 解决真实问题的人 |
| 核心理念 | 站稳脚下——过好当下 | 看见远方——成为想成为的人 |
| AI 搜索 | 智谱 GLM-4-Flash，系统提示定位为"校园生活顾问" | 智谱 GLM-4-Flash，系统提示定位为"AI 软件顾问" |
| 内容领域 | 展示层 3 领域（`daily`/`growth`/`deal`）← 数据层 8 分类（arrival/food/…），映射见 `shared/src/frontlife-constants.ts` DOMAIN_MAP | `creative`（影视创作）、`dev`（编程开发）、`work`（通用办公） |
| 种子数据 | `shared/src/frontlife-seed.ts`（8 专题 + ~67 篇文章） | `frontai-web/src/constants.ts`（Mock 数据） |
| 注册方式 | 用户名 + 密码；邮箱/手机号后置绑定 | 申请制 + 邀请制 + GitHub OAuth |
| 区域化管理 | ✅ 以校园为锚点，内容按区域组织 | ❌ 全球统一 |

#### 双站功能对照表

> 校园站已采用全新产品模型（知识库→帖子+文章），功能结构与全球站差异较大。
> 下表反映 v8 后的实际状态，非追求对称，而是各自最优解。

| 维度 | 校园站（xyzidea.cn） | 全球站（xyzidea.com） |
| --- | --- | --- |
| 产品模型 | 知识库 → 帖子 + 文章 | 领域 → 专题 → 文章 |
| 首页 | 混合 Feed（帖子 + 文章更新 + KB 更新） | Hero + AI 搜索 + 领域切换 + 内容网格 |
| AI 搜索 | 智谱 GLM-4-Flash（校园生活顾问） | 智谱 GLM-4-Flash（AI 软件顾问） |
| 内容组织 | 8 分类 → 知识库（用户归属） | 3 领域 → 专题（平台管理） |
| 文章页 | 三栏（文章列表 + TOC + 正文 + 反馈） | 三栏（目录 + 正文 + TOC） |
| 用户体系 | 统一 account + 校园 profile + 账号级 level | 统一 account + 全球 profile + 账号级 level |
| UGC 形态 | 帖子（带标签）+ 文章（Lv3+）+ 回复 | 文章（工具教程） |
| AI 写作 | 对话式 4 步引导（选KB→对话→调整→发布） | 无（Markdown 编辑器） |
| 收藏 | 文章/帖子/KB 收藏 | 文章/方案收藏 |
| 移动端导航 | 底部 3 Tab（首页/知识库/我的） | 响应式适配 |
| 敏感词管控 | @ns/shared 词库 | @ns/shared 词库 |
| 站间链接 | Header 全球站链接 | Header 校园站链接 |

#### 站间链接规则

- 用户可在两站之间自由跳转，新标签页打开
- 跳转不传递登录态（不共享跨域 Cookie/token；目标站通过本域登录态或认证服务识别同一 account）
- 跳转后可继续浏览/搜索/收藏（需在目标站注册登录）

#### 数据隔离红线（最高优先级）

以下原则**绝对不能违背**：

1. **cn 用户数据绝不流向海外节点** — 手机号等个人信息只能存于国内 PostgreSQL
2. **com 站点禁止收集任何中国敏感个人信息** — 绝对禁止：手机号、微信号、QQ号、身份证、生物特征
3. **账号身份统一但产品数据隔离** — 两站共享 account 和账号级 level，不共享业务 profile、原始行为数据或跨域 Cookie/token
4. **账号注销后 30 天内物理删除** — CDC 确保两边同步清理
5. **内容同步为单向只读** — cn 站写，com 站只读同步，不反向写入

#### 内容本地化策略

- 创作语言统一为中文（创作者母语，产出效率最高）
- 内容发布时 MUST 经过 AI 翻译（中文 → 英文），提供双语版本
- 翻译后的文章状态为"待审校"，仅标题和摘要需人工审校
- 翻译使用智谱 GLM-4-Flash 完成，不引入额外翻译服务

**原因**：双站架构兼顾国内合规和全球服务。校园站帮人站稳脚下，全球站帮人看见远方，共同服务于一个人"成为自己"的完整旅程。两站共享产品能力降低维护成本，内容差异化确保精准服务各自用户群体。

### 十二、数据平台不变量

数据平台 MUST 按角色分离视图，每个角色只看到与决策相关的数据：

#### 三角色定义

- **研究员**（内容方向决策）：关注搜索查询分析、内容覆盖率、
  阅读行为、用户来源去向、趋势变化。核心问题："下一篇该写什么？"
- **运营**（内容上线与增长）：关注内容管道状态、内容表现排名、
  用户反馈、Feature Flag 状态。核心问题："内容健康吗？"
- **开发**（系统稳定）：关注 API 性能与错误率、AI 网关用量与成本、
  部署状态。核心问题："系统正常吗？"

#### 数据采集原则

- 采集 MUST 以后端为主、前端为辅，避免前端埋点遗漏。
- 采集的数据 MUST 可追溯至具体用户行为（需用户登录）。
- 匿名流量只记录聚合指标（PV、搜索量），不记录个人行为。

#### 用户留存核心机制

- Topic 关注：用户关注感兴趣的话题，新文章发布时通知关注者。
- 这是唯一优先实现的留存机制，其他留存手段（搜索历史、个性化推荐）
  待此机制验证有效后再叠加。

**原因**：数据驱动决策需要结构化的数据采集和角色化的数据视图。
Topic 关注与三级内容结构（Domain → Topic → Article）天然契合，
实现成本低且可直接验证留存效果。

## 技术约束

| 层级   | 技术                            | 备注                       |
|-------|--------------------------------|----------------------------|
| 语言   | TypeScript（strict mode）      | 全栈统一，不引入其他语言    |
| 前端   | React + Tailwind CSS           |                            |
| 后端   | Node.js + PostgreSQL           |                            |
| 包管理 | pnpm                           | workspace monorepo         |
| 部署   | 校园站国内 ICP + 全球站海外     |                            |

## 开发工作流

1. **规格先行**：每个功能先在 `specs/` 下形成图纸，
   再进入代码实现。无规格不动代码。
2. **宪法关卡**：实现前必须逐条核对上述原则是否合规；违规项必须在计划的
   复杂度追踪表中显式标注。
3. **双站验证**：涉及两站的功能变更 MUST 同时验证校园站和全球站，
   确保功能对称性和交互一致性。
4. **共享包纯净性**：`@ns/shared` 的变更 MUST NOT 引入 React / Zustand /
   任何 UI 依赖。校园站数据文件为纯数据导出（无 store 逻辑）。
5. **渐进交付**：按优先级实现用户故事（P1 → P2 → P3），每个故事必须可独立
   测试、独立部署。
6. **缓存清理**：修改 Vite 配置或安装新依赖后，必须删除
   `node_modules/.vite` 目录再重启 dev server，避免 Outdated Optimize Dep
   错误。
7. **发布审核**：内容变更需审核员通过；技术变更需宪法合规检查。
8. **审计留痕**：所有后台操作、AI 建议、内容版本变更必须记录日志。

## 治理

本宪法高于所有其他开发实践、临时讨论与个人偏好。

**修订流程**：
- 以规格文档形式在 `specs/` 下提出修订提案。
- 提案必须包含：修订理由、受影响原则、现有代码迁移方案、审批签字。
- MAJOR 版本：原则删除或重新定义（向后不兼容）。
- MINOR 版本：新增原则或实质性扩展。
- PATCH 版本：措辞澄清、排版、笔误修正。

**合规审查**：每次 PR 或评审必须验证宪法合规性。未经论证的复杂度违规必须在合入前解决。

**运行时指引**：日常开发使用 `.specify/` 模板与命令。模板与宪法冲突时，以宪法为准。

**版本**：4.0.0 | **批准日期**：2025-12-22 | **最后修订**：2026-04-22
