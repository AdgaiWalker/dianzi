[build]
builder = "DOCKERFILE"
dockerfilePath = "NorthStar/Dockerfile.railway"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
